---
phase: 02-heuristic-improvements-onnx-feasibility
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - docs/decision-gate-onnx.md
  - docs/phase-02-summary.md
autonomous: true
requirements:
  - ACCR-01
  - ACCR-02
user_setup: []

must_haves:
  truths:
    - "ONNX decision gate thresholds are documented with measured values and pass/fail status per D-18"
    - "Fallback rule (D-19) is documented: what triggers it, what Phase 3 looks like under each path"
    - "Phase 2 results are summarized: heuristic improvement outcomes, ONNX feasibility verdict"
    - "Phase 3 recommendation is explicit: proceed with ONNX integration OR fall back to heuristic + LLM"
  artifacts:
    - path: "docs/decision-gate-onnx.md"
      provides: "ONNX decision gate documentation with thresholds, measured values, fallback rule"
      min_lines: 40
    - path: "docs/phase-02-summary.md"
      provides: "Phase 2 comprehensive summary: heuristic tuning results, ONNX spike results, Phase 3 recommendation"
      min_lines: 50
  key_links:
    - from: "docs/decision-gate-onnx.md"
      to: "D-18, D-19 in CONTEXT.md"
      via: "Threshold values and fallback rule sourced from party mode decisions"
      pattern: "D-18|D-19"
    - from: "docs/phase-02-summary.md"
      to: "runs/heuristic-tuning/results.json"
      via: "Quotes final edge-case accuracy, confusion matrix"
      pattern: "edge-case"
    - from: "docs/phase-02-summary.md"
      to: "runs/onnx-benchmark/final-verdict.json"
      via: "Quotes ONNX benchmark metrics against D-18 thresholds"
      pattern: "regression|p95|RSS"
---

<objective>
Integrate findings from Plan B (heuristic improvements) and Plan C (ONNX feasibility spike) into a decision gate document and comprehensive phase summary.

**Purpose:** Close Phase 2 with documented outcomes. Make the ONNX go/no-go decision explicit. Provide Phase 3 with a clear starting point: either "proceed with ONNX integration" or "fall back to heuristic + LLM."

**Per D-18:** Document all four decision gate thresholds with measured values.
**Per D-19:** Document the fallback rule with the evidence from 50+ warm-up benchmark runs.

**Output:**
- `docs/decision-gate-onnx.md` — ONNX decision gate documentation
- `docs/phase-02-summary.md` — Phase 2 comprehensive summary with Phase 3 recommendation
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-heuristic-improvements-onnx-feasibility/02-CONTEXT.md

**Source data (from completed Plans B & C):**
- `runs/heuristic-tuning/edge-eval-final.txt` — heuristic improvement results on edge-case corpus
- `runs/heuristic-tuning/baseline-870.json` — baseline accuracy before improvements
- `runs/heuristic-tuning/fusion-decision.md` — multi-contour fusion decision (if applicable)
- `runs/onnx-benchmark/final-verdict.json` — ONNX benchmark verdict against D-18 thresholds
- `worker/test/fixtures/cv-edge-eval/manifest.json` — 30-edge-case manifest (from Plan A)

**D-18 Thresholds (from CONTEXT.md):**
| Metric | Threshold |
|--------|-----------|
| Model binary | ≤15 MB |
| Inference p95 | ≤500 ms |
| Peak RSS | ≤80 MB |
| Cold-start | ≤2,000 ms |

**D-19 Fallback Rule:**
If ANY metric exceeds threshold consistently across 50+ warm-up runs → fall back to heuristic + LLM. Fail early: if binary >15 MB, skip measuring rest. Phase 3 scope adjustment if fallback: MAE targets revised, ~800ms LLM latency added.

**Phase 3 Requirements (for context):**
| Req | Target |
|-----|--------|
| ACCR-03 | Train regression model offline, export to ONNX |
| ACCR-04 | Integrate ONNX into CV pipeline at confidence scoring branch |
| ACCR-07 | ONNX integration test — pre-extracted features through ONNX runtime |
| ACCR-08 | MAE < 45ml, RMSE < 70ml on sealed holdout |
</context>

<tasks>

<task type="auto">
  <name>task 1: write ONNX decision gate documentation</name>
  <files>docs/decision-gate-onnx.md</files>
  <action>
    Create `docs/decision-gate-onnx.md` — a standalone reference document that captures the ONNX decision gate per D-18 and D-19.

    The document must be structured for a FRESH reader (someone who hasn't read Phase 2 context) to understand:
    - What was tested
    - What the thresholds are
    - Whether each threshold was met
    - What the fallback rule is
    - What the recommendation is for Phase 3

    ```markdown
    # ONNX Decision Gate — Phase 2 Feasibility Spike

    **Date:** 2026-05-15
    **Phase:** 02 — Heuristic Improvements & ONNX Feasibility

    ## Background

    Phase 2 conducted an ONNX feasibility spike (per ACCR-01) to determine whether
    onnxruntime-web WASM can load and run ONNX models in Cloudflare Workers within
    acceptable resource budgets. Three models were tested in staged order:

    1. **Constant output** (~10KB) — validates model loading + inference
    2. **Identity passthrough** — validates input/output tensor wiring
    3. **Small regression** (MatMul+Add, ~1KB weights) — realistic perf numbers

    ## Decision Gate Thresholds

    | # | Metric | Threshold (D-18) | Measured Value | Pass/Fail |
    |---|--------|-------------------|----------------|-----------|
    | 1 | Model binary size | ≤15 MB | {value} MB | {pass/fail} |
    | 2 | Inference p95 latency | ≤500 ms | {value} ms | {pass/fail} |
    | 3 | Peak RSS | ≤80 MB | {value} MB | {pass/fail} |
    | 4 | Cold-start time | ≤2,000 ms | {value} ms | {pass/fail} |

    *Measured values sourced from `runs/onnx-benchmark/final-verdict.json`.*
    *If binary >15 MB (metric 1), metrics 2-4 were skipped per D-19 fail-early rule.*

    ## Methodology

    - 50 warm-up runs + 5 measured runs per model
    - Runtime environment: {local Node.js v22.17.0 / Cloudflare Workers staging}
    - WASM execution provider: `onnxruntime-web` with `executionProviders: ["wasm"]`
    - Model files: `worker/src/onnx/models/{constant,identity,regression}.onnx`
    - Worker deployment: `afia-stage1.workers.dev` (via wrangler deploy)

    ## Results by Model

    ### 1. Constant Output Model
    - Binary size: {value} KB
    - Load success: {yes/no}
    - Inference success: {yes/no}
    - Notes: {e.g., "First cold load includes WASM compile time (~XXX ms)"}

    ### 2. Identity Passthrough Model
    - Binary size: {value} KB
    - I/O validation: {passed/failed — input tensor matches output tensor}
    - Notes: {e.g., "Confirmed input [{value}] produces identical output"}

    ### 3. Regression Model
    - Binary size: {value} KB
    - Cold-start load time: {value} ms
    - Inference p95 (55 runs): {value} ms
    - Measured peak RSS: {value} MB
    - Notes: {e.g., "MatMul+Add compute is negligible; bottleneck is WASM overhead"}

    ## Fallback Rule (D-19)

    **Rule:** If ANY metric exceeds threshold consistently across 50+ warm-up runs → fall back to heuristic + LLM.

    **Triggered:** {Yes / No}
    {If yes: list which metric(s) failed, and the observed values}

    **Consequence if fallback triggered:**
    - Phase 3 scope shifts from ONNX integration to heuristic refinement + LLM-only
    - MAE targets revised (LLM latency ~800ms added to response time)
    - Heuristic improvements from Phase 2 (Plan B) become the primary CV path

    ## Recommendation for Phase 3

    {One of:}
    - ✅ **Proceed with ONNX integration** — all thresholds met, Phase 3 trains regression model and integrates into pipeline
    - ⚠️ **Proceed with caveats** — most thresholds met, specific issues documented (e.g., "cold-start >2s but can relax to 5s")
    - ❌ **Fall back to heuristic + LLM** — thresholds breached, Phase 3 revises approach

    ## Raw Data

    See `runs/onnx-benchmark/final-verdict.json` for complete benchmark results.
    See `runs/onnx-benchmark/benchmark-*.json` for per-run trace data.
    See `worker/src/onnx/` for loader and benchmark source code.
    ```

    **Fill in the blanks** using the actual data from:
    - `runs/onnx-benchmark/final-verdict.json` (from Plan C)
    - `worker/src/onnx/models/*.onnx` file sizes

    If the files don't exist yet (Plan C not done), write placeholder markers like `{value}` that will be replaced when Plan C completes.

    **What to avoid:**
    - Do NOT add technical analysis beyond what the benchmark data supports
    - Do NOT include opinions — every claim must reference measured data
    - Do NOT exceed 100 lines — be concise, reference raw data files for depth
    - Do NOT modify any code files — this is a documentation-only task
  </action>
  <verify>
    <automated>Test-Path -LiteralPath "docs/decision-gate-onnx.md"</automated>
  </verify>
  <done>
    Decision gate document exists with all 4 thresholds, measured values, pass/fail per metric, fallback rule evaluation, and Phase 3 recommendation. All values either populated from benchmark data or marked as placeholders.
  </done>
</task>

<task type="auto">
  <name>task 2: write Phase 2 comprehensive summary</name>
  <files>docs/phase-02-summary.md</files>
  <action>
    Create `docs/phase-02-summary.md` — a comprehensive summary of Phase 2 outcomes, readable as a standalone reference for Phase 3 planning.

    ```markdown
    # Phase 2 Summary: Heuristic Improvements & ONNX Feasibility

    **Date:** 2026-05-15
    **Status:** Complete

    ## Goal

    Improve existing heuristic scoring to reduce confusion errors and prove ONNX
    deployment path on Workers.

    ## Plans

    | Plan | Focus | Status | Requirements |
    |------|-------|--------|--------------|
    | A (02-01) | Edge-case corpus expansion + model version tracking | {Complete} | ACCR-05, ACCR-06 |
    | B (02-03) | Heuristic scoring improvements (pre-process → tune → fusion) | {Complete} | ACCR-02 |
    | C (02-02) | ONNX feasibility spike (constant → identity → regression) | {Complete} | ACCR-01, TEST-02 |
    | D (02-04) | Decision gate + documentation (this document) | {Complete} | — |

    ## Plan A: Edge-case Corpus & Model Version Tracking

    **Outcome:** {Summary of what was delivered}

    - Coverage-gap analysis script: `worker/src/eval/coverage-gap.ts`
    - Edge-case corpus expanded from 20 to 30 images
    - Farthest-point sampling selected 10 most dissimilar images from 870-image corpus
    - Model version tracking schema: `worker/src/eval/model-version.ts`

    ## Plan B: Heuristic Improvements

    **Outcome:** {Summary — target met / not met}

    ### Baseline (before tuning)
    - Dev corpus (60 images): exact accuracy {X}%, close accuracy {Y}%
    - Edge-case (20 images → 30 after Plan A): empty↔full confusion count: {N}

    ### Pre-processing Tuning (D-17 Stage B)
    - Best configuration: CLAHE clipLimit={value}, tileSize={value}, bilateral={enabled/disabled}
    - Dev corpus regression check: exact accuracy delta = {+/-X} pp (threshold: -2pp)
    - Selected iteration: {N}

    ### Contour Scoring Tuning (D-17 Stage A)
    - Final weights: aspect={value}, center={value}, size={value}, minThreshold={value}
    - Dev corpus regression check: exact accuracy delta = {+/-X} pp (threshold: -1pp)

    ### Multi-Contour Fusion (D-17 Stage C)
    - {Implemented / Not needed — target already met}
    - {If implemented: fusion decision — kept / reverted}
    - {Rationale}

    ### Final Edge-Case Evaluation
    - Edge-case corpus: 30 images
    - Exact accuracy: {X}%
    - Close accuracy: {Y}%
    - Empty↔full confusion flips: {N} (target: ≤3 for 30 images = ≤1 per 10)
    - **ACCR-02 target met: {Yes / No}**

    ### Confusion Matrix (30 edge-case images)
    ```
    {Confusion matrix output from evaluation}
    ```

    ## Plan C: ONNX Feasibility Spike

    **Outcome:** {Summary — thresholds met / not met}

    ### Models Tested
    | Model | Size | Load Time | p95 Latency | Notes |
    |-------|------|-----------|-------------|-------|
    | Constant | {X} KB | {X} ms | {X} ms | Baseline loading test |
    | Identity | {X} KB | {X} ms | {X} ms | I/O wiring validated |
    | Regression | {X} KB | {X} ms | {X} ms | Realistic compute profile |

    ### Decision Gate Results
    | Metric | Threshold | Measured | Pass |
    |--------|-----------|----------|------|
    | Binary size | ≤15 MB | {X} MB | {Y} |
    | p95 latency | ≤500 ms | {X} ms | {Y} |
    | Peak RSS | ≤80 MB | {X} MB | {Y} |
    | Cold-start | ≤2,000 ms | {X} ms | {Y} |

    **Fallback triggered (D-19):** {Yes / No}
    **Phase 3 recommendation:** {Proceed with ONNX / Fall back to heuristic+LLM}

    ## Phase 3 Recommendations

    {Based on the decision gate outcome:}

    **If proceed with ONNX:**
    - Train regression model offline with actual bottle measurement data
    - Integrate into CV pipeline at confidence scoring branch
    - Run ONNX integration test (ACCR-07)
    - Target MAE < 45ml, RMSE < 70ml (ACCR-08)

    **If fall back to heuristic + LLM:**
    - Heuristic improvements from Phase 2 become the primary CV path
    - LLM latency (~800ms) factored into response time budget
    - MAE targets revised
    - Revisit ONNX when Workers runtime supports better WASM performance

    ## Files Changed This Phase

    {List of all files created/modified across all 4 plans, consolidated}
    ```

    **Fill in the blanks** using data from:
    - Plan A: `worker/test/fixtures/cv-edge-eval/manifest.json` (30 fixtures confirmed)
    - Plan B: `runs/heuristic-tuning/edge-eval-final.txt`, `runs/heuristic-tuning/baseline-870.json`, `runs/heuristic-tuning/fusion-decision.md`
    - Plan C: `runs/onnx-benchmark/final-verdict.json`
    - Plan D: `docs/decision-gate-onnx.md` (just written in Task 1)

    If any source data files are unavailable (plan not yet executed), use placeholder markers `{value}`.

    **What to avoid:**
    - Do NOT add opinions or speculation — every claim must reference actual data
    - Do NOT repeat decision gate details verbatim — reference the decision gate doc and summarize
    - Do NOT exceed 150 lines — be a summary, not a novel
    - Do NOT modify any code files — documentation only
  </action>
  <verify>
    <automated>Test-Path -LiteralPath "docs/phase-02-summary.md"</automated>
  </verify>
  <done>
    Phase 2 summary document exists with all 4 plan outcomes, heuristic tuning results, ONNX benchmark results, confusion matrix, decision gate verdict, and Phase 3 recommendation. All values either populated or marked as placeholders.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → filesystem | Writing markdown files only. No code execution, no data access beyond file creation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | — | docs/*.md | accept | Markdown documentation only. No code. No runtime behavior. No secrets stored. |
</threat_model>

<verification>
1. `docs/decision-gate-onnx.md` exists with all 4 thresholds and fallback evaluation
2. `docs/phase-02-summary.md` exists with all plan outcomes and Phase 3 recommendation
3. Both documents are valid markdown (no syntax errors)
4. Both documents reference actual data file paths for traceability
</verification>

<success_criteria>
1. ONNX decision gate documented with measured values per D-18
2. Fallback rule documented with trigger rationale per D-19
3. Phase 2 results comprehensively summarized
4. Phase 3 recommendation explicit and actionable
5. Both documents committed to repo
</success_criteria>

<output>
After completion, create `.planning/phases/02-heuristic-improvements-onnx-feasibility/02-04-SUMMARY.md`
</output>
